# Do NOT add any custom exclusion patterns to this file, like
#   .DS_Store/
#   .vscode/
#   .idea/
#   *~
#   *.bk
# that are generated by custom development tools or by your operating
# system. Instead exclude those files by adding the corresponding patterns
# either to your global .gitignore or only for this particular project
# in .git/info/exclude.

# Cargo build artifacts
#
# A trailing slash for the build directory has been omitted deliberately
# to allow using a symbolic link instead. This is handy when using a
# separate volume for storing temporary build artifacts.
/target
Cargo.lock
